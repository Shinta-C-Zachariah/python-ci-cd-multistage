name: Project A CI

on:
  push:
    paths:
      - "project-a/**"
      - "shared-lib/**"
  pull_request:
    paths:
      - "project-a/**"
      - "shared-lib/**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install ./shared-lib
          pip install -r project-a/requirements.txt

      - name: Run tests
        run: pytest project-a/tests